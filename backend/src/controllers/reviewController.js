const Review = require("../models/Review"); exports.getReviews = async (req, res) => { const reviews = await Review.find().populate("user").populate("business"); res.json({ success: true, data: reviews }); }; exports.createReview = async (req, res) => { req.body.user = req.user.id; const review = await Review.create(req.body); res.status(201).json({ success: true, data: review }); };
